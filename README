Ophion multibot.

Config:
    bot.py (top)
    classes.py (class Cache)

Pre-requisites:
    Python, with MySQLdb library (pip install MySQL-python)

Chat (suggestions/comments/whatever):
    irc.quakenet.org #mustis.dev

In action:
    irc.quakenet.org #jobbig
